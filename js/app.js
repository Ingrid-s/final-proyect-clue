/*var testArray = [];
for(var i = 0; i <= 10 ; i+=1){
  /*Agregar elementos al arreglo en la última posición
  testArray.push(i);
  console.log (testArray);
  // eliminar el primer elemento del array
  testArray.shift();
  console.log(testArray);
  //Eliminar ultimo elemento del arreglo
  testArray.pop();
  console.log(testArray);
  //Revertir los elementos del arreglo
  testArray.reverse();
  console.log(testArray);
}

/*---------------------------------------------------------*/
var testString = "abcde";
testString.charAt(2);
/*para obtener un acaracter a partir de su codigo Ascii
utilzo el metodo string.fromCharCode()
para obtener el codogio ascii de un caracter utilizo String.chaCodeAt(indice)*/
var codigo = testString.charCodeAt(2);
console.log(codigo);
// 99
string.fromCharCode(codigo);
// c
